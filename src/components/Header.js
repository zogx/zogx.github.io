import { HeaderContainer, HeaderTitle } from './Header.style';

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle variant="h4">
        zogx
      </HeaderTitle>
    </HeaderContainer>
  );
};

export default Header;
